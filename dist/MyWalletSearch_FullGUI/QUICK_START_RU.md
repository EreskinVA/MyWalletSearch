# 🚀 Быстрый старт - Сегментированный поиск для Puzzle 71

## За 5 минут до запуска

### 1️⃣ Компиляция (Linux/macOS)

```bash
cd VanitySearch
make clean
make all
```

Или с GPU (если есть NVIDIA CUDA):
```bash
make gpu=1 CCAP=7.5 all
```

### 2️⃣ Базовый запуск

```bash
# Простой тест (будет искать префикс "1Test")
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 4 1Test

# Поиск для Puzzle 71 (реальный адрес)
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 8 1FshYo

# С GPU
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 1FshYo
```

### 3️⃣ Что происходит?

Программа будет искать приватные ключи в диапазоне 71-бит (2^70 до 2^71-1) в заданных сегментах:
- **Сегмент 1**: 48-54% (поиск вверх ↑)
- **Сегмент 2**: 56-54% (поиск вниз ↓)
- **Сегмент 3**: Зона около 53% (горячая зона)
- И другие...

## 📊 Создание своей конфигурации

Создайте файл `my_segments.txt`:

```
# Мои сегменты для поиска
45.0 54.0 up    поиск_вверх_от_45
59.0 54.0 down  поиск_вниз_от_59
```

Запуск:
```bash
./VanitySearch -seg my_segments.txt -bits 71 -t 8 1FshYo
```

## 🎯 Целевая информация Puzzle 71

```
Адрес:     1FshYoP92x7zKbW93VDmXqYjXhH1t5Sq2r
Диапазон:  2^70 до 2^71 - 1
Награда:   0.71 BTC (на момент создания puzzle)
```

## ⚡ Оптимизация производительности

### CPU оптимизация
```bash
# Используйте количество физических ядер
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 16 1FshYo
```

### GPU оптимизация
```bash
# Один GPU
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 1FshYo

# Несколько GPU
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -gpuId 0,1 -t 8 1FshYo

# С настройкой сетки
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -g 256,512 -t 8 1FshYo
```

### Вывод в файл
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 -o results.txt 1FshYo
```

## 📋 Форматы конфигурации

### Базовый формат
```
startPercent endPercent direction [name]
```

### Примеры направлений

**Поиск ВВЕРХ** (от меньших ключей к большим):
```
45.0 55.0 up segment_ascending
```

**Поиск ВНИЗ** (от больших ключей к меньшим):
```
60.0 50.0 down segment_descending
```

**Встречный поиск** (два сегмента навстречу друг другу):
```
48.0 54.0 up    from_below
56.0 54.0 down  from_above
```

## 🔍 Стратегии поиска

### Консервативная (фокус на вероятной зоне)
```
48.0 54.0 up    core_zone
52.0 53.5 up    hot_spot
```

### Агрессивная (широкое покрытие)
```
40.0 50.0 up    lower_half
50.0 60.0 up    upper_half
```

### Встречная (максимальное покрытие центра)
```
45.0 52.5 up    ascending
57.5 52.5 down  descending
```

## 🎓 Понимание процентов

Для Puzzle 71:
- **0%** = `0x400000000000000000` (2^70, минимум диапазона)
- **50%** = `0x600000000000000000` (середина)
- **100%** = `0x7FFFFFFFFFFFFFFFFFF` (2^71-1, максимум диапазона)

Наши прогнозы указывают на зону **48-55%** как наиболее вероятную.

## ⚠️ Важно знать

1. **Это займет время**: даже с мощным GPU поиск может занять месяцы
2. **Нет гарантий**: статистика - это вероятность, не гарантия
3. **Следите за температурой**: GPU под нагрузкой греется
4. **Электричество**: высокая нагрузка = высокое потребление энергии

## 📈 Интерпретация вывода

```
[258.45 Mkey/s][GPU 241.23 Mkey/s][Total 2^38.21][Prob 1.2%][50% in 00:15:42]

258.45 Mkey/s     - Общая скорость (млн ключей/сек)
GPU 241.23 Mkey/s - Скорость GPU
Total 2^38.21     - Проверено ключей
Prob 1.2%         - Вероятность, что ключ был в проверенных
50% in 00:15:42   - Время до 50% вероятности
```

## 🆘 Проблемы?

### "Error: -seg requires -bits parameter"
Всегда используйте оба параметра:
```bash
./VanitySearch -seg config.txt -bits 71 <prefix>
```

### "Cannot open segment file"
Проверьте путь к файлу:
```bash
ls -la segments_puzzle71.txt
./VanitySearch -seg ./segments_puzzle71.txt -bits 71 1FshYo
```

### Программа вылетает
Попробуйте без GPU сначала:
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 4 1FshYo
```

## 🎯 Готовые команды

### Для слабого ПК (только CPU, 4 потока)
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 4 -o found.txt 1FshYo
```

### Для среднего ПК (CPU + старая GPU)
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 -o found.txt 1FshYo
```

### Для мощного ПК (современная GPU, много ядер)
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 16 -g 512,256 -o found.txt 1FshYo
```

### Для серверной/майнинговой фермы (несколько GPU)
```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -gpuId 0,1,2,3 -t 32 -o found.txt 1FshYo
```

## 🎉 Если найдете ключ!

Программа автоматически выведет:
```
PubAddress: 1FshYoP92x7zKbW93VDmXqYjXhH1t5Sq2r
Priv (WIF): p2pkh:Kxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
Priv (HEX): 0xXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

**Сразу же**:
1. Скопируйте приватный ключ в безопасное место
2. Проверьте баланс адреса
3. Импортируйте ключ в Bitcoin кошелек
4. Переведите средства на свой адрес

## 📚 Дополнительно

- Полная документация: `SEGMENT_SEARCH_README_RU.md`
- Примеры конфигураций: `segments_config_example.txt`
- Оптимизированная конфигурация: `segments_puzzle71.txt`

---

**Удачи! 🍀🔑**


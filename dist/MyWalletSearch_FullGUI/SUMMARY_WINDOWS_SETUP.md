# Итоговая сводка: Настройка VanitySearch на Windows

## ✅ Выполнено

### 1. Проверка конфигурации
- **Видеокарта**: NVIDIA GeForce GTX 1050 Ti
- **Compute Capability**: 6.1 (Pascal)
- **CUDA**: v13.1 (установлена)
- **Путь CUDA**: `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1`

### 2. Созданные файлы
- `build_windows_gpu.ps1` - скрипт автоматической сборки
- `Makefile.windows` - Makefile для Windows
- `setup_compiler.ps1` - скрипт проверки компилятора
- `BUILD_WINDOWS_RU.md` - подробная инструкция
- `QUICK_SETUP_WINDOWS.md` - быстрая инструкция

## ⚠️ Требуется действие

### Установка компилятора C++

Выберите один из вариантов:

#### Вариант 1: MSYS2 (рекомендуется, ~5 минут)

1. Скачайте: https://www.msys2.org/
2. Установите в `C:\msys64`
3. Откройте **MSYS2 MinGW 64-bit** (важно: не обычный MSYS2!)
4. Выполните:
   ```bash
   pacman -Syu
   pacman -S mingw-w64-x86_64-gcc
   ```
5. Закройте терминал

#### Вариант 2: Visual Studio Build Tools

1. Скачайте: https://visualstudio.microsoft.com/downloads/
2. Выберите "Build Tools for Visual Studio"
3. Установите "Desktop development with C++"

## 🚀 Следующие шаги

После установки компилятора:

### 1. Проверка компилятора
```powershell
.\setup_compiler.ps1
```

### 2. Сборка проекта
```powershell
.\build_windows_gpu.ps1
```

Скрипт автоматически:
- Проверит CUDA
- Найдет компилятор
- Соберет проект с GPU поддержкой для CCAP=6.1
- Создаст `VanitySearch.exe`

### 3. Проверка сборки
```powershell
.\VanitySearch.exe -g 32,64 -check
```

Если видите ошибку OOM, уменьшите grid:
```powershell
.\VanitySearch.exe -g 16,64 -check
```

### 4. Тестовый запуск GPU поиска

```powershell
# Быстрый тест (найдет результат быстро)
.\VanitySearch.exe -gpu -gpuId 0 -g 32,64 -t 2 -m 2000000 -o test.txt "1P*X"

# Или с префиксом
.\VanitySearch.exe -gpu -gpuId 0 -g 32,64 -t 2 -m 2000000 -o test.txt "18ss"
```

## 📊 Рекомендуемые параметры для GTX 1050 Ti

- **Grid**: `-g 32,64` или `-g 16,64` (зависит от доступной памяти)
- **Потоки CPU**: `-t 2` (можно 4-8)
- **MaxFound**: `-m 2000000`
- **GPU ID**: `-gpuId 0`

## 🔧 Устранение проблем

### "Compiler not found"
- Установите MSYS2 или Visual Studio Build Tools
- Проверьте путь: `C:\msys64\mingw64\bin\g++.exe`

### "CUDA not found"
- Проверьте путь: `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1`
- Установите CUDA Toolkit 13.1

### "Kernel out of memory"
- Уменьшите grid: `-g 32,64` → `-g 16,64` → `-g 8,64`
- Закройте другие приложения, использующие GPU

### "items lost"
- Увеличьте `-m`: `-m 2000000` → `-m 5000000`
- Или уменьшите grid

## 📝 Параметры сборки

- **Compute Capability**: 6.1 (GTX 1050 Ti)
- **CUDA Version**: 13.1
- **Архитектура**: x86_64
- **Оптимизация**: -O3

## 📚 Дополнительная информация

См. также:
- `RUNBOOK_RU.md` - полная инструкция по использованию
- `BUILD_WINDOWS_RU.md` - подробная инструкция по сборке
- `QUICK_SETUP_WINDOWS.md` - быстрая инструкция


# 🔍 Мониторинг GPU и оптимизация параметра `-g`

## Быстрая проверка

### 1. Текущая загрузка GPU

```bash
# Один раз
nvidia-smi

# В реальном времени (обновление каждую секунду)
watch -n 1 nvidia-smi

# Только ключевые метрики
nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.total,power.draw,temperature.gpu --format=csv
```

### 2. Автоматический анализ с рекомендациями

```bash
./check_gpu_load.sh
```

Скрипт покажет:
- GPU Utilization (%)
- Memory Usage (%)
- Power Draw (%)
- Рекомендации по увеличению/уменьшению `-g`

---

## Ключевые метрики для анализа

### ✅ **МОЖНО УВЕЛИЧИТЬ `-g`**, если:

1. **GPU-Util < 90%** → GPU недогружен
2. **Memory Usage < 80%** → есть запас памяти
3. **Power Draw < 85% от TDP** → не достигнут лимит мощности
4. **В логах НЕТ `items lost`** → буфер не переполняется
5. **Temperature < 80°C** → нет термодроссинга

### ⚠️ **НЕ УВЕЛИЧИВАЙТЕ `-g`**, если:

1. **GPU-Util ≥ 95%** → уже максимальная загрузка
2. **Memory Usage ≥ 90%** → близко к OOM
3. **Power Draw ≥ 95%** → достигнут TDP
4. **В логах есть `items lost`** → нужно уменьшить `-g` или увеличить `-m`
5. **Temperature ≥ 83°C** → возможен термодроссинг

---

## Пошаговая оптимизация `-g`

### Текущая конфигурация (RTX 4090)

```bash
-g 64,128    # X=64 блоков, Y=128 потоков в блоке
             # Итого: 8192 потока
```

### Эксперимент 1: Увеличить X

```bash
-g 96,128    # +50% блоков → 12288 потоков
             # Мониторьте: GPU-Util, items lost
```

### Эксперимент 2: Ещё больше X

```bash
-g 128,128   # +100% блоков → 16384 потоков
             # Осторожно: проверьте память и items lost
```

### Эксперимент 3: Увеличить Y (редко помогает)

```bash
-g 64,256    # +100% потоков в блоке → 16384 потоков
             # Может ухудшить производительность из-за регистров
```

---

## Проверка логов VanitySearch

### Проверить наличие `items lost`

```bash
# В реальном времени
tail -f log_*.log | grep -i "items lost"

# Исторические данные
grep -i "items lost" log_*.log
```

**Если видите `items lost`:**
- ❌ Уменьшите `-g` (например, `64,128` → `48,128`)
- ✅ ИЛИ увеличьте `-m` (например, `2000000` → `4000000`)

### Проверить keyrate (ключей/сек)

```bash
tail -n 50 log_*.log | grep -i "key"
```

**Сравнение:**
- Запустите с `-g 64,128` → запишите keyrate
- Запустите с `-g 96,128` → сравните keyrate
- Если keyrate вырос → увеличение оправдано
- Если keyrate упал → вернитесь к меньшему `-g`

---

## Пример: Оптимизация для RTX 4090

### Шаг 1: Базовая конфигурация

```bash
./VanitySearch -gpu -gpuId 0 -g 64,128 -m 2000000 \
               -seg seg.txt -bits 71 -o out.txt 1PWo3JeB
```

**Мониторинг:**
```bash
# В другом терминале
watch -n 1 nvidia-smi
```

**Ожидаемо для RTX 4090:**
- GPU-Util: 95-98%
- Memory: ~6000-8000 MiB / 24564 MiB
- Power: ~350-400W / 450W
- Temp: 65-75°C

### Шаг 2: Если GPU-Util < 90%

```bash
./VanitySearch -gpu -gpuId 0 -g 96,128 -m 3000000 \
               -seg seg.txt -bits 71 -o out.txt 1PWo3JeB
```

**Проверьте:**
- GPU-Util должен вырасти до ≥ 92%
- Нет `items lost` в логах
- Keyrate не упал

### Шаг 3: Если всё хорошо → попробуйте больше

```bash
./VanitySearch -gpu -gpuId 0 -g 128,128 -m 4000000 \
               -seg seg.txt -bits 71 -o out.txt 1PWo3JeB
```

**Осторожно:**
- Если появились `items lost` → уменьшите `-g` или увеличьте `-m`
- Если keyrate упал → вернитесь к предыдущему значению

---

## Типичные проблемы

### Проблема: "items lost"

**Симптомы:**
```
Warning, 12345 items lost, increase -m or reduce -g
```

**Решения:**
1. Увеличить `-m`: `-m 2000000` → `-m 4000000`
2. Уменьшить `-g`: `-g 128,128` → `-g 96,128`
3. Уменьшить количество сегментов (если их много)

### Проблема: GPU-Util < 80%

**Симптомы:**
- GPU работает не на полную мощность
- Keyrate ниже ожидаемого

**Решения:**
1. Увеличить `-g`: `-g 64,128` → `-g 96,128`
2. Увеличить `-t` (CPU потоки): `-t 2` → `-t 4`
3. Проверить, что паттерн не слишком сложный (wildcards замедляют)

### Проблема: Out of Memory (OOM)

**Симптомы:**
```
CUDA out of memory
```

**Решения:**
1. Уменьшить `-g`: `-g 128,128` → `-g 64,128`
2. Уменьшить `-m`: `-m 4000000` → `-m 2000000`
3. Закрыть другие GPU-процессы

---

## Команды для быстрой диагностики

```bash
# 1. Текущая загрузка
nvidia-smi

# 2. Детальный анализ
./check_gpu_load.sh

# 3. Проверка логов на ошибки
tail -n 100 log_*.log | grep -E "items lost|error|warning" -i

# 4. Мониторинг в реальном времени
watch -n 1 './check_gpu_load.sh'

# 5. Сравнение keyrate (запустите с разными -g и сравните)
grep -i "key.*/sec" log_*.log | tail -5
```

---

## Оптимальные значения для RTX 4090

**Рекомендуемая стартовая конфигурация:**
```bash
-g 64,128 -m 2000000 -t 2
```

**Если есть запас по метрикам:**
```bash
-g 96,128 -m 3000000 -t 2
```

**Максимальная (если нет `items lost`):**
```bash
-g 128,128 -m 4000000 -t 2
```

**⚠️ ВАЖНО:** Всегда проверяйте лог на `items lost` и сравнивайте keyrate!


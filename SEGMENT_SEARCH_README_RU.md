# VanitySearch - Режим сегментированного поиска

## 🎯 Описание

Модифицированная версия VanitySearch с поддержкой **сегментированного поиска**. Позволяет искать Bitcoin ключи в заданных сегментах диапазона с возможностью параллельного поиска в нескольких направлениях.

## 🆕 Новые возможности

- **Сегментированный поиск**: задайте несколько диапазонов в процентах от полного пространства ключей
- **Двунаправленный поиск**: ищите "вверх" (от меньших к большим) или "вниз" (от больших к меньшим)
- **Параллельная обработка**: несколько сегментов обрабатываются параллельно
- **Конфигурационные файлы**: легко настраиваемые текстовые файлы с сегментами
- **Оптимизация для Puzzle**: предустановленные конфигурации для Bitcoin Puzzle 71

## 📦 Новые файлы

- `SegmentSearch.h` / `SegmentSearch.cpp` - реализация сегментированного поиска
- `segments_config_example.txt` - пример конфигурации
- `segments_puzzle71.txt` - оптимизированная конфигурация для Puzzle 71
- `SEGMENT_SEARCH_README_RU.md` - данная инструкция

## 🔨 Компиляция

### Linux (без GPU)

```bash
cd VanitySearch
make clean
make all
```

### Linux (с GPU/CUDA)

```bash
cd VanitySearch
make clean
make gpu=1 CCAP=7.5 all
```

Замените `7.5` на compute capability вашей GPU:
- RTX 30xx: 8.6
- RTX 20xx: 7.5
- GTX 10xx: 6.1
- GTX 9xx: 5.2

### macOS (без GPU)

```bash
cd VanitySearch
make clean
make all
```

**Примечание**: CUDA на macOS не поддерживается Apple с 2017 года.

## 📝 Формат конфигурационного файла

```
# Комментарии начинаются с #
# Формат: startPercent endPercent direction [name]

# Поиск вверх от 45% до 54%
45.0 54.0 up segment_up_45-54

# Поиск вниз от 59% до 54%
59.0 54.0 down segment_down_59-54

# Можно задать любое количество сегментов
48.0 52.0 up hotzone_center
```

**Параметры:**
- `startPercent` - начало сегмента (0.0 - 100.0)
- `endPercent` - конец сегмента (0.0 - 100.0)
- `direction` - направление: `up`/`вверх` или `down`/`вниз`
- `name` - имя сегмента (опционально)

## 🚀 Использование

### Базовый синтаксис

```bash
./VanitySearch -seg <config_file> -bits <bit_range> [другие опции] <prefix>
```

### Примеры

#### Пример 1: Поиск для Puzzle 71 с готовой конфигурацией

```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 -o results.txt 1
```

#### Пример 2: Поиск с пользовательской конфигурацией

```bash
./VanitySearch -seg my_segments.txt -bits 71 -gpu -gpuId 0,1 -t 16 1BitCoin
```

#### Пример 3: Только CPU, без GPU

```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -t 16 -o output.txt 1
```

#### Пример 4: Поиск в известном адресе Puzzle 71

```bash
./VanitySearch -seg segments_puzzle71.txt -bits 71 -gpu -t 8 1FshYo
```

### Новые параметры командной строки

- `-seg <file>` - файл конфигурации сегментов (обязательный для сегментированного поиска)
- `-bits <n>` - битовый диапазон (например, 71 для puzzle 71) (обязательный с `-seg`)

### Все параметры

```
-v              Вывести версию
-u              Искать несжатые адреса
-b              Искать оба типа (сжатые и несжатые)
-c              Поиск без учета регистра
-gpu            Включить GPU
-stop           Остановиться при нахождении всех префиксов
-i <file>       Файл со списком префиксов
-o <file>       Выходной файл для результатов
-gpuId <ids>    Список GPU через запятую (по умолчанию: 0)
-g <grid>       Размер сетки GPU
-m <n>          Макс. количество найденных ключей
-s <seed>       Начальное значение (seed)
-ps <seed>      Seed с добавлением криптостойкого случайного числа
-t <n>          Количество CPU потоков
-nosse          Отключить SSE
-r <n>          Интервал обновления ключа (rekey) в мегаключах
-seg <file>     Файл конфигурации сегментов
-bits <n>       Битовый диапазон для сегментов
```

## 📊 Конфигурация для Puzzle 71

Файл `segments_puzzle71.txt` оптимизирован на основе статистического анализа и содержит:

1. **Центральная зона (встречное движение)**: 48-54% и 56-54%
2. **Зона максимальной вероятности**: около 53%
3. **Зона взвешенной средней**: около 50.6%
4. **Расширенный поиск**: 45-48% и 56-59%

### Диапазон Puzzle 71

```
От: 2^70 = 1180591620717411303424
До: 2^71-1 = 2361183241434822606847
```

### Целевой адрес Puzzle 71

```
1FshYoP92x7zKbW93VDmXqYjXhH1t5Sq2r
```

## 💡 Советы по использованию

### Для максимальной производительности:

1. **Используйте GPU**: добавьте `-gpu` флаг
2. **Оптимизируйте потоки**: `-t` = количество физических ядер - количество GPU
3. **Несколько GPU**: `-gpuId 0,1,2` для использования нескольких GPU
4. **Настройте сетку GPU**: экспериментируйте с `-g` для оптимального размера

### Стратегии сегментирования:

1. **Встречное движение**: 
   ```
   48.0 52.0 up
   56.0 52.0 down
   ```

2. **Концентрические зоны**:
   ```
   49.0 51.0 up   # Внутренняя зона
   47.0 53.0 up   # Средняя зона
   45.0 55.0 up   # Внешняя зона
   ```

3. **Параллельные полосы**:
   ```
   45.0 48.0 up
   48.0 51.0 up
   51.0 54.0 up
   54.0 57.0 up
   ```

## ⚠️ Важные замечания

1. **Вычислительная сложность**: Puzzle 71 требует ~2^70 операций в среднем
2. **Время поиска**: может занять месяцы/годы даже на мощном оборудовании
3. **Сегменты не ускоряют поиск**: они позволяют сфокусироваться на вероятных зонах
4. **Статистика не гарантирует успех**: ключ может быть в любом месте диапазона

## 📈 Мониторинг прогресса

Программа выводит:
- Скорость поиска (MKey/s)
- Проверенные ключи
- Вероятность нахождения
- Статус сегментов

Пример вывода:
```
=== Segment Search Mode ===
Config file: segments_puzzle71.txt
Bit range: 71
==========================

[SegmentSearch] Загружено сегментов: 6
Сегмент 1: central_up (48.00% -> 54.00%, ВВЕРХ ↑)
Сегмент 2: central_down (56.00% -> 54.00%, ВНИЗ ↓)
...

[258.45 Mkey/s][GPU 241.23 Mkey/s][Total 2^38.21][Prob 1.2%]
```

## 🔧 Решение проблем

### Ошибка компиляции

```bash
# Убедитесь, что все зависимости установлены
sudo apt-get install build-essential g++ make

# Для GPU - установите CUDA toolkit
```

### "Error: -seg requires -bits parameter"

Всегда указывайте оба параметра вместе:
```bash
./VanitySearch -seg config.txt -bits 71 <prefix>
```

### Низкая производительность GPU

- Проверьте compute capability: `-l` для списка GPU
- Настройте размер сетки: `-g 256,512` (экспериментируйте)
- Убедитесь, что CUDA drivers актуальны

### Программа не находит конфигурационный файл

Используйте полный путь:
```bash
./VanitySearch -seg /full/path/to/segments.txt -bits 71 <prefix>
```

## 📚 Примеры конфигураций

### Узкий целевой поиск (50-54%)

```
50.0 51.0 up narrow_1
51.0 52.0 up narrow_2
52.0 53.0 up narrow_3
53.0 54.0 up narrow_4
```

### Широкий поиск с встречными направлениями

```
40.0 50.0 up wide_low_up
60.0 50.0 down wide_high_down
```

### Максимальное покрытие (рискованно - медленнее)

```
0.0 25.0 up quarter_1
25.0 50.0 up quarter_2
50.0 75.0 up quarter_3
75.0 100.0 up quarter_4
```

## 🎓 Дополнительные ресурсы

- Оригинальный VanitySearch: https://github.com/JeanLucPons/VanitySearch
- Bitcoin Puzzle: https://bitcointalk.org/index.php?topic=1306983.0
- SECP256K1: https://en.bitcoin.it/wiki/Secp256k1

## 📄 Лицензия

Как и оригинальный VanitySearch, распространяется под лицензией GPL v3.

## ⚡ Производительность

Типичная производительность:
- CPU (Intel i9-12900K, 16 потоков): ~15-25 MKey/s
- GPU (RTX 3090): ~800-1200 MKey/s
- GPU (RTX 4090): ~1500-2000 MKey/s

Для Puzzle 71 при скорости 1000 MKey/s:
- Один сегмент 10% = ~37 лет в среднем
- Focused search на "горячую зону" может сократить время

---

**Удачи в поиске! 🔑🎰**


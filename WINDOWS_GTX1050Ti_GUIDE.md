# 🪟 Запуск на Windows с GTX 1050 Ti

## 📋 Системные требования

- Windows 10/11
- NVIDIA GTX 1050 Ti (4GB VRAM)
- Visual Studio 2017/2019/2022
- CUDA SDK 11.0+

---

## 🔨 Компиляция

### Вариант 1: Visual Studio (GUI)

1. Откройте `VanitySearch.sln` в Visual Studio
2. Выберите конфигурацию: **Release** (не Debug!)
3. Build → Rebuild Solution
4. Исполняемый файл: `x64/Release/VanitySearch.exe`

### Вариант 2: Командная строка (если есть build tools)

```cmd
cd C:\path\to\MyWalletSearch
msbuild VanitySearch.sln /p:Configuration=Release /p:Platform=x64
```

---

## 🚀 Запуск для Puzzle 71

### PowerShell команды:

```powershell
cd C:\path\to\MyWalletSearch\x64\Release

# Проверка GPU
.\VanitySearch.exe -l

# Запуск с оптимизированной конфигурацией
.\VanitySearch.exe -seg ..\..\segments_GTX1050Ti.txt -bits 71 `
  -kangaroo `
  -progress puzzle71.dat -autosave 600 `
  -gpu -gpuId 0 `
  -g 256,128 `
  -t 4 `
  -o PUZZLE_71_SOLUTION.txt `
  1PWo3JeB
```

### Или в CMD:

```cmd
cd C:\path\to\MyWalletSearch\x64\Release

VanitySearch.exe -seg ..\..\segments_GTX1050Ti.txt -bits 71 -kangaroo -progress puzzle71.dat -autosave 600 -gpu -gpuId 0 -g 256,128 -t 4 -o PUZZLE_71_SOLUTION.txt 1PWo3JeB
```

---

## ⚙️ Оптимальные настройки для GTX 1050 Ti

**GPU Grid Size:**
```
-g 256,128   # Стандартный (рекомендую)
-g 192,128   # Если перегрев
-g 320,128   # Для максимума (если охлаждение хорошее)
```

**CPU Threads:**
```
-t 4   # Оставьте 4 потока CPU для GPU управления
-t 2   # Если у вас 4-ядерный CPU
-t 6   # Если 8+ ядер
```

---

## 📊 Ожидаемая производительность

**GTX 1050 Ti:**
- Скорость: ~100-150 MKey/s (GPU)
- CPU: ~10-20 MKey/s
- Итого: ~110-170 MKey/s

**Время для Puzzle 71 (с Kangaroo):**
- Зона 48-55%: **~2-4 месяца**
- Полный диапазон: **~1-2 года**

---

## 💡 РЕКОМЕНДАЦИИ

### Для GTX 1050 Ti:

**1. Thermal Management:**
```
- Следите за температурой (MSI Afterburner)
- Если > 80°C - уменьшите grid: -g 192,128
- Установите temperature limit: nvidia-smi -pl 75
```

**2. Экономия электричества:**
```
- GTX 1050 Ti потребляет ~75W
- За месяц: ~54 kWh
- Считайте экономическую целесообразность vs облако
```

**3. Стабильность:**
```
- Используйте автосохранение: -autosave 600
- Регулярно проверяйте: type puzzle71.dat
- Backup прогресса: copy puzzle71.dat puzzle71_backup.dat
```

---

## 🎯 ИТОГОВАЯ РЕКОМЕНДАЦИЯ

### ❌ GTX 1050 Ti для Puzzle 71:
- Слишком медленно (месяцы/год)
- Не окупается vs облако

### ✅ Лучше:
- Vast.ai с 14x RTX 4090: **12-24 часа** за $112-224
- Или аренда 4x RTX 3090: **2-4 дня** за $200-400

### ⚡ Но если всё равно хотите на GTX 1050 Ti:

**Команда выше работает!** Просто будьте готовы ждать месяцы.

---

## 📝 Структура конфигурации для кастомизации

### Создайте свой файл:

```
# my_segments.txt

# Ваши сегменты
40.0 50.0 up segment1
50.0 60.0 up segment2
60.0 70.0 up segment3
```

**Каждая строка** = один сегмент для поиска  
**Потоки распределяются** между сегментами автоматически  
**Балансировка** происходит динамически

---

**Полный адрес для копирования:** `1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU` ✅

# 🎮 Оптимизация для GTX 1050 Ti 4GB

## 📊 Характеристики GPU

- **Модель**: NVIDIA GeForce GTX 1050 Ti
- **Архитектура**: Pascal (GP107)
- **Compute Capability**: 6.1 (sm_61)
- **VRAM**: 4 GB GDDR5
- **CUDA Cores**: 768
- **Memory Bandwidth**: 112 GB/s

## ✅ Настройки проекта

Проект уже оптимизирован для GTX 1050 Ti:

### Рекомендуемая конфигурация сборки

**Для максимальной производительности:**
- Используйте конфигурацию **`ReleaseSM30`** (оптимизирована для sm_61)
- Или **`Release`** (поддерживает все архитектуры, включая sm_61)

**Для отладки:**
- Используйте **`Debug`** (теперь также оптимизирована для sm_61)

### Code Generation

Проект настроен на компиляцию для:
- ✅ **sm_61** - GTX 1050 Ti (Pascal)
- ✅ Поддержка других архитектур для совместимости

## 🚀 Рекомендации по использованию

### 1. Оптимальные параметры запуска

Для GTX 1050 Ti с 4GB VRAM рекомендуется:

```bash
# Базовый запуск с GPU
VanitySearch.exe -gpu -gpuId 0 -g 256,256

# С сегментами (оптимизировано для GTX 1050 Ti)
VanitySearch.exe -gpu -gpuId 0 -g 256,256 -segments segments_GTX1050Ti.txt

# Для диапазона 54-62 бит (оптимизировано)
VanitySearch.exe -gpu -gpuId 0 -g 256,256 -segments segments_54-62_GTX1050Ti.txt
```

### 2. Grid Size (размер сетки)

**Рекомендуемые значения для GTX 1050 Ti:**
- `-g 256,256` - оптимальный баланс
- `-g 512,256` - если нужно больше параллелизма
- `-g 128,128` - для экономии памяти (если не хватает VRAM)

**⚠️ Не используйте слишком большие значения:**
- Максимум: `-g 1024,512` (может вызвать нехватку памяти)
- При ошибках "out of memory" уменьшите grid size

### 3. Сегментированный поиск

Используйте предварительно настроенные файлы сегментов:

**`segments_GTX1050Ti.txt`** - оптимизирован для одной GPU:
- Фокус на зоне 48-55% (самая вероятная)
- 4 сегмента для эффективного использования ресурсов

**`segments_54-62_GTX1050Ti.txt`** - для диапазона 54-62 бит:
- Оптимизирован для GTX 1050 Ti
- Сбалансированное покрытие диапазона

### 4. Параметры производительности

**Ожидаемая производительность GTX 1050 Ti:**
- **~50-80 MKey/s** (миллионов ключей в секунду)
- Зависит от сложности паттерна и настроек

**Оптимизация:**
- Используйте `-rekey` для периодической смены ключей
- Включите `-useSSE` для дополнительного ускорения CPU
- Используйте сегментированный поиск для лучшего покрытия

## 🔧 Настройки CUDA для GTX 1050 Ti

### Compute Capability

GTX 1050 Ti использует **sm_61** (Pascal архитектура):
- ✅ Поддерживается CUDA 13.1
- ✅ Оптимизированные инструкции для Pascal
- ✅ Эффективное использование 768 CUDA ядер

### Memory Management

С 4GB VRAM:
- ✅ Достаточно для большинства задач
- ⚠️ Следите за использованием памяти при больших grid size
- 💡 Используйте `-gpuId 0` для явного указания GPU

## 📈 Мониторинг производительности

### Проверка работы GPU

```bash
# В другом окне PowerShell/CMD
nvidia-smi
```

Должно показать:
- Использование GPU: ~95-100%
- Использование памяти: зависит от grid size
- Температура: нормальная рабочая (60-75°C)

### Оптимальные показатели

- **GPU Utilization**: 95-100% ✅
- **Memory Usage**: 1-3 GB (из 4 GB) ✅
- **Power Usage**: ~75W (типично для GTX 1050 Ti)
- **Temperature**: < 80°C ✅

## ⚠️ Ограничения и рекомендации

### Ограничения GTX 1050 Ti

1. **4GB VRAM** - ограничивает максимальный grid size
2. **768 CUDA Cores** - меньше, чем у более новых карт
3. **Pascal архитектура** - не поддерживает некоторые новые оптимизации

### Рекомендации

1. **Не перегружайте GPU:**
   - Избегайте grid size > 1024x512
   - Мониторьте температуру

2. **Используйте сегменты:**
   - Более эффективное использование ресурсов
   - Лучшее покрытие пространства ключей

3. **Оптимизируйте паттерны:**
   - Простые паттерны работают быстрее
   - Сложные паттерны требуют больше вычислений

## 🐛 Решение проблем

### Проблема: "out of memory"
**Решение:**
- Уменьшите grid size: `-g 128,128` или `-g 256,128`
- Закройте другие приложения, использующие GPU
- Используйте меньшие сегменты

### Проблема: Низкая производительность
**Решение:**
- Проверьте, что используется ReleaseSM30 или Release конфигурация
- Убедитесь, что драйверы NVIDIA обновлены
- Проверьте температуру GPU (throttling при перегреве)

### Проблема: GPU не используется
**Решение:**
- Убедитесь, что указан `-gpu` флаг
- Проверьте `-gpuId 0` (если несколько GPU)
- Проверьте, что CUDA правильно установлена: `nvcc --version`

## 📚 Дополнительные ресурсы

- [NVIDIA GTX 1050 Ti Specifications](https://www.nvidia.com/en-us/geforce/products/10series/geforce-gtx-1050-ti/)
- [CUDA Pascal Architecture](https://docs.nvidia.com/cuda/pascal-tuning-guide/)
- Файлы сегментов: `segments_GTX1050Ti.txt`, `segments_54-62_GTX1050Ti.txt`

## ✅ Итоговая конфигурация

**Для GTX 1050 Ti рекомендуется:**

1. **Сборка**: ReleaseSM30 или Release
2. **Grid Size**: `-g 256,256`
3. **Сегменты**: Используйте `segments_GTX1050Ti.txt`
4. **Мониторинг**: Следите за температурой и использованием памяти

**Пример оптимального запуска:**
```bash
VanitySearch.exe -gpu -gpuId 0 -g 256,256 -segments segments_GTX1050Ti.txt -o results.txt
```


# 🚀 ИНСТРУКЦИЯ: ОПТИМИЗИРОВАННЫЙ ПОИСК PUZZLE #71 (160 сегментов)

## 📋 КРАТКАЯ СВОДКА

**ЦЕЛЬ:** `1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU`  
**НАГРАДА:** 7.10 BTC (~$600,000 USD)  
**ДИАПАЗОН:** 71-73% от 2^70 (оптимизированный!)  
**СЕГМЕНТОВ:** 160 (плотное покрытие)  
**ПАТТЕРН:** `1PWo3JeB9*` (9 символов, высокая специфичность)

---

## ⚡ БЫСТРЫЙ СТАРТ (на сервере)

### 1. Загрузите файлы на сервер

```bash
# Скопируйте эти 2 файла на сервер:
# - seg_optimal_160_71_73.txt
# - run_optimal_160_gpu.sh

# Например, через scp:
scp seg_optimal_160_71_73.txt user@server:~/MyWalletSearch/
scp run_optimal_160_gpu.sh user@server:~/MyWalletSearch/
```

### 2. Подготовка на сервере

```bash
ssh user@server
cd ~/MyWalletSearch

# Сделайте скрипт исполняемым
chmod +x run_optimal_160_gpu.sh

# Проверьте наличие файлов
ls -lh seg_optimal_160_71_73.txt run_optimal_160_gpu.sh
```

### 3. Запуск

```bash
# ЗАПУСК (в фоне с nohup)
./run_optimal_160_gpu.sh

# Проверьте, что процесс запустился
ps aux | grep VanitySearch
```

### 4. Мониторинг

```bash
# Следить за логом в реальном времени
tail -f log_optimal_160_71_73.log

# Проверить прогресс (простой)
./show_segment_progress_simple.sh seg_optimal_160_71_73.txt progress_optimal_160_71_73.dat

# Проверить прогресс (детальный)
python3 show_segment_progress.py seg_optimal_160_71_73.txt progress_optimal_160_71_73.dat out_optimal_160_71_73.txt

# Посчитать найденные адреса
grep -c 'PubAddress:' out_optimal_160_71_73.txt 2>/dev/null || echo '0'

# Проверить GPU нагрузку
nvidia-smi -l 1
```

---

## 🎯 ПОЧЕМУ ЭТОТ ДИАПАЗОН ОПТИМАЛЬНЫЙ?

### КРИТИЧЕСКОЕ ОТКРЫТИЕ:

В предыдущих поисках (hex_87_89_*) найдено **50 адресов** с префиксом `1PWo3J`:

- ✅ `1PWo3JeB6XZ...` ← ОЧЕНЬ БЛИЗКО к целевому `1PWo3JeB9`!
- ✅ `1PWo3JeBjDE...` ← ОЧЕНЬ БЛИЗКО!

### Base58 алфавит:
```
...456789ABCDEF...
     ↑   ↑      ↑
     6   9      j
   (pos 5) (pos 8) (pos 42)
```

### ВЫВОД:
`1PWo3JeB9` находится **МЕЖДУ** найденными `1PWo3JeB6` и `1PWo3JeBj`!

Эти адреса были найдены в диапазоне **71.5-72.5%**, НЕ в 87-89%!

**→ Старая стратегия искала НЕ ТАМ (смещение на 15-17%!)**  
**→ Новая стратегия: 71-73% = ПРАВИЛЬНЫЙ диапазон!**

---

## 📊 ПАРАМЕТРЫ ОПТИМИЗАЦИИ

### Диапазон:
- **Start:** `0x6d70a3d70a3d700000` (dec: 2,018,811,671,426,773,286,912)
- **End:**   `0x6eb851eb851eb80000` (dec: 2,042,423,503,841,121,533,952)
- **Размер:** 23.6 квинтиллионов ключей (2% от полного 71-битного)

### Сегменты:
- **Количество:** 160 сегментов
- **Размер сегмента:** ~147.6 квадриллионов ключей каждый
- **Направления:** Чередуются up/down для равномерного покрытия

### Паттерн:
- **`1PWo3JeB9*`** - 9 символов
- **Специфичность:** В 58 раз выше, чем `1PWo3JeB*` (8 символов)
- **Ожидаемые находки:** Адреса типа `1PWo3JeB9[x]...`

---

## 🔧 НАСТРОЙКА GPU

### Параметры в скрипте:

```bash
GPU_ID=0                    # Один GPU
# GPU_ID=0,1,2,3            # Несколько GPU (раскомментируйте)

GRID="128,256"              # Для RTX 4090
# GRID="64,128"             # Если OOM (out of memory)
# GRID="32,128"             # Если всё ещё OOM

MAXFOUND=5000000            # Большой буфер (5 миллионов)
AUTOSAVE_INTERVAL=300       # Автосохранение каждые 5 минут
```

### Проверка GPU:

```bash
# Информация о GPU
nvidia-smi

# Compute Capability (для сборки)
nvidia-smi --query-gpu=compute_cap --format=csv,noheader

# Мониторинг в реальном времени
watch -n 1 nvidia-smi
```

### Если OOM (Out of Memory):

1. Уменьшите `-g`: `GRID="64,128"` или `GRID="32,128"`
2. Уменьшите `-m`: `MAXFOUND=2000000`
3. Закройте другие процессы на GPU

---

## 📈 ОЦЕНКА ВРЕМЕНИ

При скорости **1 GKey/s** (одна RTX 4090):

| Паттерн          | Пространство поиска | Время (оценка)      |
|------------------|---------------------|---------------------|
| `1PWo3J*`        | ~2.3e21 / 58^1      | ~12,700 лет         |
| `1PWo3JeB*`      | ~2.3e21 / 58^2      | ~220 лет            |
| `1PWo3JeB9*`     | ~2.3e21 / 58^3      | ~3.8 года           |
| `1PWo3JeB9j*`    | ~2.3e21 / 58^4      | ~24 дня             |

**С 10 GPU @ 1 GKey/s каждый:** делите на 10!

**С Kangaroo method:** ещё в ~1000 раз быстрее (если реализован)!

---

## 🛑 ОСТАНОВКА ПОИСКА

```bash
# Остановить поиск
pkill -f 'VanitySearch.*seg_optimal_160_71_73'

# Или по PID (смотрите в выводе скрипта)
kill <PID>

# Проверить, что остановлен
ps aux | grep VanitySearch
```

**⚠️ ВАЖНО:** Прогресс сохраняется автоматически каждые 5 минут в `progress_optimal_160_71_73.dat`

---

## 📦 BACKUP И RESUME

### Резервная копия прогресса:

```bash
# Создать backup
cp progress_optimal_160_71_73.dat progress_optimal_160_71_73.dat.backup.$(date +%Y%m%d_%H%M%S)

# Или автоматически в cron
*/30 * * * * cd ~/MyWalletSearch && cp progress_optimal_160_71_73.dat progress_optimal_160_71_73.dat.backup.$(date +\%Y\%m\%d_\%H\%M\%S)
```

### Resume (продолжение):

```bash
# Скрипт автоматически обнаруживает progress файл и использует -resume
./run_optimal_160_gpu.sh

# Если нужно начать заново (ОСТОРОЖНО - потеряете прогресс!):
rm progress_optimal_160_71_73.dat
./run_optimal_160_gpu.sh
```

---

## 🎯 ПРОВЕРКА РЕЗУЛЬТАТА

### При нахождении адресов:

```bash
# Показать все найденные адреса
grep 'PubAddress:' out_optimal_160_71_73.txt

# Проверить, найден ли ЦЕЛЕВОЙ адрес
grep '1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU' out_optimal_160_71_73.txt

# Если найден - вывести всю информацию
grep -A 15 '1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU' out_optimal_160_71_73.txt
```

### Анализ найденных адресов:

```bash
# Статистика по 8-му символу (после 1PWo3JeB)
grep 'PubAddress: 1PWo3JeB' out_optimal_160_71_73.txt | cut -c 18-18 | sort | uniq -c | sort -rn

# Топ-10 адресов, наиболее близких к целевому
python3 << 'EOF'
target = "1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU"
with open('out_optimal_160_71_73.txt', 'r') as f:
    addrs = [line.split(': ')[1].strip() for line in f if 'PubAddress:' in line]

matches = []
for addr in addrs:
    match_len = sum(1 for a, b in zip(addr, target) if a == b)
    matches.append((addr, match_len))

matches.sort(key=lambda x: x[1], reverse=True)
print("ТОП-10 наиболее близких:")
for addr, match_len in matches[:10]:
    print(f"{addr} | {match_len} символов")
EOF
```

---

## 🚨 TROUBLESHOOTING

### Проблема: VanitySearch не запускается

```bash
# Проверьте сборку
./VanitySearch -check

# Пересоберите с правильным CCAP
make clean
make gpu=1 CCAP=8.9 all -j$(nproc)  # Для RTX 4090
```

### Проблема: "items lost" в логе

**Причина:** Переполнение буфера находок

**Решение:**
1. Увеличьте `-m`: `MAXFOUND=10000000`
2. Или уменьшите `-g`: `GRID="32,128"`

### Проблема: Медленная скорость

```bash
# Проверьте GPU нагрузку
nvidia-smi

# Должно быть:
# - GPU Utilization: ~95-100%
# - Memory Usage: высокое, но не OOM

# Если низкая нагрузка - увеличьте -g:
GRID="256,256"  # или больше
```

### Проблема: Прогресс не сохраняется

**Проверьте:** [[memory:12622084]]  
В SegmentSearch прогресс для GPU должен обновляться по глобальным threadId.

```bash
# Проверьте прогресс файл
ls -lh progress_optimal_160_71_73.dat

# Посмотрите содержимое
grep -E "Name=|Direction=|CurrentKey=" progress_optimal_160_71_73.dat | head -20
```

---

## 💡 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### 1. Параллельный запуск на нескольких GPU:

Создайте отдельные конфигурации для каждого GPU:

```bash
# GPU 0: сегменты 1-40
sed -n '1,40p' seg_optimal_160_71_73.txt > seg_gpu0.txt

# GPU 1: сегменты 41-80
sed -n '41,80p' seg_optimal_160_71_73.txt > seg_gpu1.txt

# И т.д...
```

### 2. Мониторинг через Telegram/Discord:

```bash
# Отправка уведомлений при нахождении адресов
tail -f out_optimal_160_71_73.txt | while read line; do
    if [[ "$line" == *"1PWo3JeB9"* ]]; then
        # Отправить в Telegram/Discord
        curl -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" \
            -d chat_id=<CHAT_ID> \
            -d text="🎯 Найден адрес: $line"
    fi
done &
```

### 3. Облачные GPU (Vast.ai, RunPod):

- Дешевле, чем собственные GPU
- Легко масштабировать до 10-100 GPU
- Скрипт работает без изменений

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Файлы в проекте:

- `seg_optimal_160_71_73.txt` - Файл сегментов (160 штук)
- `run_optimal_160_gpu.sh` - Скрипт запуска
- `out_optimal_160_71_73.txt` - Результаты (создаётся автоматически)
- `log_optimal_160_71_73.log` - Лог выполнения
- `progress_optimal_160_71_73.dat` - Прогресс (для resume)

### Полезные ссылки:

- RUNBOOK: `RUNBOOK_RU.md`
- Анализ паттернов: `analyze_found_addresses.py`
- Визуализация прогресса: `show_segment_progress.py`

---

## 🏆 ПРИ УСПЕШНОМ НАХОЖДЕНИИ

### ✅ Если найден целевой адрес:

1. **НЕМЕДЛЕННО** остановите поиск: `pkill -f VanitySearch`
2. **Сделайте backup** всех файлов: `tar -czf FOUND_$(date +%Y%m%d).tar.gz *.txt *.dat *.log`
3. **Проверьте** приватный ключ из файла результатов
4. **Импортируйте** в Bitcoin кошелёк (осторожно!)
5. **Переведите** средства на безопасный адрес

### Формат результата:

```
PubAddress: 1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU
Priv (WIF): [приватный ключ в WIF формате]
Priv (HEX): [приватный ключ в HEX]
Priv (DEC): [приватный ключ в DEC]
...
```

**⚠️ БЕЗОПАСНОСТЬ:** Никому не показывайте приватный ключ до перевода средств!

---

## 📞 ПОДДЕРЖКА

При проблемах проверьте:
1. `RUNBOOK_RU.md` - детальная инструкция
2. Логи: `tail -100 log_optimal_160_71_73.log`
3. GPU статус: `nvidia-smi`

---

**🚀 УДАЧИ В ПОИСКЕ! MAY THE ODDS BE EVER IN YOUR FAVOR! 🎯**

---

*Создано: Декабрь 2025*  
*Оптимизация: На основе криптографического анализа найденных адресов*


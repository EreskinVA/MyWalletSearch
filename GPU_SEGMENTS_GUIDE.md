# GPU-специфичные сегменты - Руководство по реализации

## 📋 Обзор

GPU-специфичные сегменты позволяют распределять разные диапазоны ключей между GPU и CPU для максимальной эффективности.

## 🎯 Концепция

GPU оптимальны для:
- Последовательных диапазонов ключей
- Больших блоков вычислений
- Параллельной обработки

CPU оптимальны для:
- Разрозненных проверок
- Небольших сегментов
- Динамического переключения

## 🔧 Текущая реализация

### SegmentSearch поддерживает:
- ✅ Базовое распределение сегментов
- ✅ Балансировка нагрузки между потоками
- ⚠️  GPU использует те же сегменты что и CPU

### Что нужно для полной GPU поддержки:

1. **Модификация GPUEngine.cu**
   - Передача диапазонов ключей в GPU kernel
   - Изменение логики генерации ключей в GPU
   
2. **Адаптер SegmentSearch → GPU**
   - Конвертация сегментов в GPU-совместимый формат
   - Распределение сегментов между GPU устройствами

3. **Синхронизация**
   - Обновление прогресса GPU сегментов
   - Балансировка между GPU и CPU

## 🚀 Быстрый старт (текущая функциональность)

GPU в текущей версии использует:
```cpp
// В Vanity.cpp - GPU получает те же начальные ключи
void VanitySearch::getGPUStartingKeys(int thId, int groupSize, int nbThread, 
                                       Int *keys, Point *p) {
  // Использует segmentSearch->GetStartingKey() если включено
}
```

## 📊 Пример использования

```bash
# GPU + CPU с сегментами (текущая версия)
./VanitySearch -seg segments_puzzle71.txt -bits 71 \\
               -gpu -gpuId 0,1 -t 8 1FshYo

# GPU получит ключи из тех же сегментов что и CPU
```

## 🔮 Будущие улучшения

### Фаза 1: Базовая GPU интеграция (РЕАЛИЗОВАНО частично)
- [x] Передача сегментов в конструктор VanitySearch
- [x] Использование GetStartingKey для GPU потоков
- [ ] GPU-специфичные диапазоны

### Фаза 2: Оптимизированное распределение
- [ ] Большие блоки для GPU
- [ ] Мелкие сегменты для CPU
- [ ] Автоматическая оптимизация

### Фаза 3: Адаптивная работа
- [ ] Динамическое переключение GPU/CPU
- [ ] Измерение производительности
- [ ] Автоматическое перераспределение

## 💡 Рекомендации

### Для максимальной производительности сейчас:

1. **Используйте крупные сегменты для GPU**
   ```
   # segments_gpu_optimized.txt
   45.0 55.0 up large_segment_1
   ```

2. **Распределите GPU и CPU**
   ```bash
   # 2 GPU + 8 CPU потоков
   ./VanitySearch -seg segments.txt -bits 71 \\
                  -gpu -gpuId 0,1 -t 8 1FshYo
   ```

3. **Мониторьте производительность**
   - Проверяйте скорость GPU vs CPU
   - Настраивайте gridSize для GPU
   - Используйте LoadBalancer статистику

## 🔬 Техническая информация

### Архитектура GPU в VanitySearch:

```
GPU/GPUEngine.cu
    ├─ ComputeKeys()          // Генерация ключей
    ├─ CheckHash()            // Проверка хешей
    └─ LaunchSearch()         // Основной цикл

Изменения для сегментов:
    ├─ Передать startKey из сегмента
    ├─ Ограничить endKey границей сегмента
    └─ Синхронизация с SegmentSearch
```

### Пример модификации (концепт):

```cpp
// В GPUEngine.cu (будущая реализация)
__global__ void comp_keys_segment(
    uint64_t *keys,
    uint32_t *compressed,
    Int segmentStart,    // Новый параметр
    Int segmentEnd,      // Новый параметр
    Int incr
) {
    Int key;
    key.Set(&segmentStart);
    key.Add(threadIdx.x + blockIdx.x * blockDim.x);
    
    // Проверка границ сегмента
    if (key.IsGreater(&segmentEnd)) return;
    
    // ... остальной код ...
}
```

## 📚 Дополнительные ресурсы

- VanitySearch GPU: `GPU/GPUEngine.cu`
- Segment Search: `SegmentSearch.cpp`
- Load Balancer: `LoadBalancer.cpp`
- Примеры: `segments_puzzle71.txt`

## ⚠️ Важно

Текущая реализация **работает** с GPU, но не оптимизирована для сегментированного поиска. GPU получает начальные ключи из сегментов, но не учитывает границы сегментов при генерации.

Для полной оптимизации требуется:
1. Модификация CUDA kernels
2. Тестирование на разных GPU
3. Профилирование производительности

## 🎯 Статус

- ✅ Базовая интеграция (работает)
- ⚠️  Оптимизация (в процессе)
- ⏳ Полная поддержка (планируется)

---

*Версия: 1.0*  
*Дата: 23 декабря 2025*

